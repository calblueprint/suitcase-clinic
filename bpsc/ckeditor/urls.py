from django.conf.urls import patterns
from django.conf.urls import url

from bpsc.ckeditor.views import *

urlpatterns = patterns('',
	# url(r'^post/$', PostView.as_view(), name='post'),
	# Put anything in this file?
)